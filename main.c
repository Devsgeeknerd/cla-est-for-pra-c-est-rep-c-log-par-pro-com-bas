#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numero = 0;
    printf("Digite o numero: ");
    scanf("%d", &numero);
    int = 0;
    for (i = 0; i <= 10; i++)
    {
        printf("%d + %d = %d\n", numero, i, numero + i);
        printf("%d - %d = %d\n", numero, i, numero - i);
        printf("%d x %d = %d\n", numero, i, numero * i);
        printf("%d / %d = %d\n", numero, i, numero / i);
    }
    return 0;
}
